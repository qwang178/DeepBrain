scale.R         scale the data based on ROSMAP deep learning dataset (R code)
manifold.m      obtain manifold representation for the intermediate layer (matlab code)
predict.R       unsupervised dimensionality reduction (PCA + UMAP transform), pseudotime(SI) calculation for 3D UMAP trajectory (R code)
